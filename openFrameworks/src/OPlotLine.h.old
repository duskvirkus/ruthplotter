#pragma once

#include "OPlot.h"
#include "ofMain.h"

namespace oplot {

class OpenPlotLine {
public:
  OpenPlotLine();
  ~OpenPlotLine();

  void command(COMMAND);
  void uint16(int byteOffset, u_int16_t);
  void float32(int byteOffset, float);

private:
  byte lineBytes[OPLOT_LINE_SIZE];
};

} // namespace oplot
